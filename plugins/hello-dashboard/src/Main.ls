(module Plugins.HelloDashboard.Main)

;; This is intentionally minimal until generated L#frame SDK bindings land.
;; The first real plugin will contribute a view, command, and hot-reload state.

(defn main []
  0)
