---
description: L#frame リポジトリの正本方針
alwaysApply: true
---

# 正本方針

計画、編集、レビュー、コマンド実行の前に @AGENTS.md を読み、従う。
最も近いスコープ別 `AGENTS.md` が領域固有の指示を追加する。
ユーザーが別言語を指定しない限り、AI の自然言語出力は日本語にする。

Cursor rule にリポジトリ方針を重複定義してはいけない。この rule は Cursor をツール非依存の
正本指示へ接続するだけに留める。
