---
description: L# Kernel と Plugin の規則
globs:
  - "kernel/**/*.ls"
  - "plugins/**/*.ls"
  - "plugins/**/*.toml"
alwaysApply: false
---

@AGENTS.md を読む。Kernel 変更では @kernel/AGENTS.md、Plugin 変更では @plugins/AGENTS.md を読む。
host 連携を Effect として表し、Capability 検査を通す。説明、レビュー、引き継ぎは日本語で出力する。
