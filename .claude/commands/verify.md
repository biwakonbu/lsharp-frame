Verify the current working tree without changing product behavior.

1. Read `AGENTS.md` and scoped instructions.
2. Inspect `git status` and `git diff`.
3. Run `make doctor`, `make harness`, and `make ci` when available.
4. Map failures to root causes; do not suppress warnings or relax gates.
5. Report commands, pass/fail status, unavailable tools, and residual risk.
