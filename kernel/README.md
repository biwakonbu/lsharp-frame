# L#frame Kernel

This directory will contain the canonical L# application kernel once `lsharp:frame` WIT bindings are wired.

The source currently proves only repository layout and ordinary L# compilation shape. It does not yet export the WIT lifecycle interface.
