(module LsharpFrame.Kernel)

;; Milestone 0 skeleton. The WIT lifecycle adapter will call these functions
;; after L# Component bindings are introduced.

(defn init []
  0)

(defn dispatch [state event-count]
  (+ state event-count))

(defn main []
  0)
