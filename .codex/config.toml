# プロジェクト単位の Codex 既定値。モデル、プロバイダー、認証、テレメトリーは
# ユーザー単位の設定とし、このリポジトリにはコミットしない。
approval_policy = "on-request"
sandbox_mode = "workspace-write"
project_doc_max_bytes = 65536
project_root_markers = [".git", "Cargo.toml"]

[features]
multi_agent = true
